services:
  - type: web
    name: phytoai-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements_backend.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
